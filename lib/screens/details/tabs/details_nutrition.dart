import 'package:flutter/material.dart';

class ProductNutrition extends StatelessWidget {
  const ProductNutrition({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Center(
        child: Text('Nutrition'),
      ),
    );
  }
}
